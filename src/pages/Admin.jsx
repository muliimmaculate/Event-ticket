import React from "react";

const Admin = () => (
  <div className="admin-container">
    <h1 className="admin-title">Admin Panel</h1>
    <p className="admin-desc">Admin features (add/edit/delete events, view bookings) will be available here.</p>
  </div>
);

export default Admin; 